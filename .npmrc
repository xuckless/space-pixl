# The PIXL engine binding is a private package on GitHub Packages.
# Locally: export GITHUB_PACKAGES_TOKEN=<PAT with read:packages>.
# In CI the workflows set it from the PACKAGES_TOKEN secret.
@xuckless:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_PACKAGES_TOKEN}
