# Update feed used when running unpackaged with SPACE_PIXL_FORCE_UPDATER=1.
# Mirrors `publish` in electron-builder.yml: the GitHub releases of this
# repository. The repository is public, so no token is needed to read them.
provider: github
owner: xuckless
repo: space-pixl
