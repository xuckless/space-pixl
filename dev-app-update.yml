# Update feed used when running unpackaged with SPACE_PIXL_FORCE_UPDATER=1.
# Mirrors the first `publish` entry in electron-builder.yml (electron-updater
# does not expand env vars here, so the URL is spelled out). Reads go through
# the bucket's public URL; R2's S3 endpoint only answers signed requests.
provider: generic
url: https://pub-a43a48ef06ba489fb058d7378d921856.r2.dev/space-pixl
useMultipleRangeRequest: false
